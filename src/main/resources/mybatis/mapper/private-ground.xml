<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.testserverapi.repository.PrivateGroundRepository">

    <insert id="save" parameterType="com.example.testserverapi.model.vo.PrivateGround">
        INSERT INTO private_ground ( host_id, title, description, type, unit_amount, access_vehicle,
                                    space_size, theme, created_at, created_by, deleted )
         VALUES ( #{hostId}, #{title}, #{description}, #{type}, #{unitAmount}, #{accessVehicle},
             #{spaceSize}, #{theme}, now(), #{createdBy}, #{deleted} )
    </insert>

    <select id="findById" parameterType="long" resultType="com.example.testserverapi.model.vo.PrivateGround">
        SELECT * FROM private_ground WHERE host_id = #{hostId}
    </select>
</mapper>