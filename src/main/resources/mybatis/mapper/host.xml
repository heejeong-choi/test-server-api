<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.testserverapi.repository.HostRepository">

    <resultMap type="Host" id="Host">
        <result column="host_id" property="hostId" />
        <result column="name" property="name" />
        <result column="email" property="email" />
        <result column="image_url" property="imageUrl" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <insert id="save" parameterType="com.example.testserverapi.model.vo.Host">
        INSERT INTO host
        (
         name, email, image_url, created_at, created_by, deleted
        )
        VALUES
        (
        #{name}, #{email}, #{imageUrl}, NOW(), #{name} ,false
        )
    </insert>

    <select id="get" parameterType="Long" resultType="com.example.testserverapi.model.dto.HostResDto">
        SELECT
            host_id, name, email, image_url, created_at
        FROM
            host
        WHERE
            host_id = #{hostId}
    </select>

</mapper>